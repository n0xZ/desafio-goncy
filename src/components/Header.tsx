import { Component } from 'solid-js';

const Header: Component = () => {
	return (
		<header class="border-b-2 border-gray-300 mb-4 p-4">
			<nav class="container mx-auto font-bold text-xl">
				Estampitiency
			</nav>
		</header>
	);
};

export default Header;
